<div class="text-center mt-4">
  <h1 class="mt-5 text-center">採点結果</h1>
  <p>正答数は <%= @correct_count %> 問中 <%= @total_count %> 問です。</p>
  <% incorrect_count = @total_count - @correct_count %>
  <p><%= incorrect_count %> 問不正解です。</p>
  <div class="mt-4">
    <%= link_to 'トップに戻る', root_path, class: "btn btn-primary" %>
  </div>
</div>
